project:
  name: Basal-UTUC-spatial-ecosystems
  repository: https://github.com/zzzqi123/Basal-UTUC-spatial-ecosystems
  release: v0.1-reviewer

paths:
  input_dir: data/processed
  output_dir: outputs
  public_data_dir: data/public

runtime:
  seed: 20260730
  threads: 4

samples:
  spatial:
    - sample_id: SLZ
      group: NMI-Basal
    - sample_id: WJ
      group: MI-Basal
    - sample_id: YJM
      group: MI-Basal

privacy:
  allow_patient_level_clinical_data: false
  allow_large_binary_objects: false

